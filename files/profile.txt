# MANAGED BY PUPPET!
if [ -d /usr/local/zookeeper ]; then
  export ZOOKEEPER_HOME=/usr/local/zookeeper
  export PATH=${ZOOKEEPER_HOME}/bin:${PATH}
fi

if [ -d /var/zookeeper ]; then
  export ZOOCFGDIR=/var/zookeeper
  export ZOODATA=${ZOOCFGDIR}/data
fi